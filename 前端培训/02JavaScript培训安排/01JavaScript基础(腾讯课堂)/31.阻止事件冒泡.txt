一、什么是事件冒泡？
在标签中嵌套了标签，父标签和子标签上都有对应的事件，在子标签上触发了子标签的事件，会不会导致父标签上的这个事件也执行

二、阻止事件冒泡的事件：
1、stopPropagation()：它就是阻止事件冒泡(火狐、谷歌浏览器、IE无效)
2、cancelBubble：返回布尔值，指示事件是否可拥可取消的默认动作。
3、preventDefault：通知浏览器不要执行与事件关联的默认动作。
4、confirm：显示带有一段消息以及确认按钮和取消按钮的对话框。