@using _001_HelloWord.Models;@*引入命名空间*@
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Show</title>
</head>
<body>
    <div> 
        <h1>Show</h1>
        <hr />
        @*  这样定义的@后面存放的是C#代码*@
        @{ 
            int a1 = 10;
            //输出
            Response.Write(a1);
            //【注意】：页面里面不能定义方法
        }
        <br />
        @* 输出 *@
        @* 当输出遇到HTML代码时会自动结束输出，后面的不会在当做变量输出了 *@
        @a1<br />123
        @* 使用for循环输出偶数 *@
        @* 在Razor中支持C#大吗与html混合编写 *@
        @for(int i = 0; i < 10; i++)
        {//他会自己去匹配大括号
            //大括号匹配就近原则
            if (i%2==0)
            {
                //输出必须需要使用@符号
                <span>123</span>
                if (i%3==0)
                {
                    @i//输出变量的时候不需要加分号
                }
            }
        }
        @*服务器端注释：不会放到输出流中，也就是在前台页面的源代码中看不到*@
        <!--客户端注释：会放到输出流中，在前台页面的源代码中可以看到注释内容-->
        @{ 
            Person p1 = new Person()
            {
                age = 10
            };
        }
        @p1.age
    </div>
</body>
</html>
