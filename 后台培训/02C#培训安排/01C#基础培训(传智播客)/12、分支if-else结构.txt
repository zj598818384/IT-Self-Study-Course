一、顺序结构：程序从main函数进入，从上到下一行一行的执行，不会落下任何一行。

二、分支结构：if、if-else

三、选择结构：if、else-if、switch-case

四、循环结构：which、do-which、for、foreach

五、if语句：
语法：
if（判断条件）
{
      要执行的代码；
}
判断条件：一般为关系表达式或者bool类型的值
执行过程：程序运行到if处，首先判断if所带的小括号中的判断条件，如果条件成立，也就是返回true，则执行if所带的大括号中的代码，如果判断条件不成立，也就是返回一个false，则跳过if结构，继续向下执行。